feat(auth): 实现完整的用户认证与RBAC权限系统

本次提交实现了全面的用户认证与RBAC权限控制系统，主要包含以下工作：

- **后端**：
  - 实现了JWT认证、安全中间件、CORS配置。
  - 建立了完整的RBAC角色与权限模型。
  - 提供了用户、角色、权限管理的API端点。

- **前端**：
  - 实现了登录、注册、密码重置页面。
  - 引入了基于角色的路由守卫与权限指令（v-permission, v-role）。
  - 完善了用户状态管理（Pinia）。

- **文档**：
  - 新增了 `用户认证功能说明.md`，包含系统架构、快速启动指南和API文档。